# Phase 6A5 Corrected 12-Layout Run: Stopped at Hard Gates

This is a new namespace and does not modify the immutable Phase 6A4 failure
root. It completed 12 layouts, 96 scenes, and 288 route executions from the
fixed-color manifest (`577e7867...`).

The amendment's hard gates still fail. Fixed colors removed the permutation
bug, but hidden obstacles belonging to other route bits remain visible in some
camera images. Consequently the actual image hash partition is richer than the
state-view-observation table: same contracted outcome does not imply the same
image. `branch_dev_04` retains the clear-left physical collision. Finally, the
runner did not promote its subprocess replay result into the formal summary;
the root is provisional for provenance purposes.

The symbolic adaptive gap (11/12 layouts) is retained only as diagnostic output.
No adaptive scientific claim is promoted, and RGB, continuous geometry, PF, and
learned-particle work remain closed.
